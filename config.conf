# DATABASE CONFIGURATION
# Fill out all database variables to configure the database connection
# Currently only postgres
# If no database variables are filled out, data will be saved to a local json file 

DB_USER=""
DB_PASSWORD=""
DB_HOST=""
DB_PORT=""
DB_NAME=""


# TUI OPTIONS
ENABLE_TUI="false"
