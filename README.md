# CLI To-Do App Made With GO
Application to experiment with bubbletea in go to create a TUI

# Install
1) Clone the Git repository
2) Change directory into GoToDoCLI/Go
3) Run "Make"

Optional
4) Move binary to PATH location
